# 2050 Fitness App

Static mobile-first training tracker.

Open `index.html` from any static web server. The app stores user data in browser localStorage and supports JSON import/export from the home page.
